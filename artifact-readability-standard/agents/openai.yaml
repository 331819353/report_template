interface:
  display_name: "交付物可读性标准"
  short_description: "设计人类可评审、AI可抽取的交付结构，并将截图转成文本契约。"
  default_prompt: "Use $artifact-readability-standard to make delivery artifacts human-readable and AI-extractable, including converting screenshots or visual references into text-based contracts with stable IDs, fields, evidence, gaps, and one canonical source of truth."
